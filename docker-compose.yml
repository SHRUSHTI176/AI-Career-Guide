version: "3.8"
services:
  backend:
    build: ./backend-ai
    command: uvicorn app:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./backend-ai:/app
    env_file:
      - ./backend-ai/.env

  # Frontend via npm dev server - optional to dockerize
